import 'package:flutter/material.dart';

class TopRightSection extends StatelessWidget {
  const TopRightSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 55,
    );
  }
}
